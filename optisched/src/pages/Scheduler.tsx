import { Outlet, Link } from "react-router-dom";

function Scheduler() {
    return (
        <div className="page_main">
            <h1 className="title">
                Hello World! Enter the Scheduler...
            </h1>
        </div>
    );
};

export default Scheduler